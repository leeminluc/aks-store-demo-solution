{{- if .Values.productService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "aks-store-demo.labels" . | nindent 4 }}
    app: product-service
spec:
  type: {{ .Values.productService.service.type }}
  ports:
    - port: {{ .Values.productService.service.port }}
      targetPort: {{ .Values.productService.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "aks-store-demo.selectorLabels" . | nindent 4 }}
    app: product-service
{{- end }}
